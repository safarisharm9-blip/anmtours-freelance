import { NO_INDEX_METADATA } from "@/config/seo";

export const metadata = NO_INDEX_METADATA;

export default function AddServiceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
